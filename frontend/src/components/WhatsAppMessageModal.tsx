import React from 'react';
// WhatsAppMessageModal is no longer applicable with the new backend.
// This is a stub to prevent import errors.
interface Props {
  open?: boolean;
  onOpenChange?: (open: boolean) => void;
  customer?: unknown;
  delivery?: unknown;
}
export default function WhatsAppMessageModal(_props: Props) {
  return null;
}
