import React from 'react';
// DeliveryQuantityChart is no longer applicable with the new backend (no delivery records).
// This is a stub to prevent import errors.
export default function DeliveryQuantityChart() {
  return (
    <div className="flex items-center justify-center h-48 text-muted-foreground text-sm">
      Delivery chart not available
    </div>
  );
}
