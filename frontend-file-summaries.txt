{
  "frontend/src/index.css": "Global stylesheet with warm earthy OKLCH color system; adds a `@media print` block that hides `.print:hidden` elements, resets backgrounds/colors to black-on-white, and ensures table borders and page-break rules are print-friendly.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Nunito/Merriweather fonts, farm color palette tokens, custom card shadows, and all standard shadcn/ui theme mappings.",
  "frontend/src/hooks/useQueries.ts": "Central React Query hooks file with all backend actor calls plus exported time utility functions (`dateToNanoseconds`, `nanosecondsToDate`, `getTodayNanoseconds`); all mutations properly invalidate their respective query caches on success.",
  "frontend/src/components/Layout.tsx": "App shell with sticky navigation bar (includes all seven nav links with icons), mobile hamburger menu, main content area, and branded footer; updated the logo to use the correct `ao-farms-logo.dim_400x400.png` asset (the user's actual A.O. Farms buffalo-head logo) at height 48px.",
  "frontend/src/components/CattleForm.tsx": "Reusable cattle add/edit form with a useRef submission guard, status Select dropdown (Active/Inactive), health status Select with conditional sick-detail fields, and all fields wired to the backend Cattle type.",
  "frontend/src/components/InventoryForm.tsx": "Reusable form component supporting both add-item (name, category, quantity, unit) and update-quantity modes for inventory management.",
  "frontend/src/components/MilkProductionChart.tsx": "Recharts bar chart showing 7-day milk production aggregated from MilkRecord data; removed the unused `nanosecondsToDate` import (uses inline bigint math instead).",
  "frontend/src/pages/Dashboard.tsx": "Enhanced Dashboard with 8 stat cards (including Active Customers, Milk This Month, All-Time Milk, Deliveries This Month), a dismissible alerts/notifications panel for low production and missing deliveries, the existing 7-day chart, a new CattleHealthChart, and the recent milk records table.",
  "frontend/src/pages/CattleManagement.tsx": "Cattle management page with stats cards, table, and add/edit dialogs; replaced the non-existent `Cow` icon with `Beef` from lucide-react.",
  "frontend/src/pages/MilkProduction.tsx": "Milk production page with log form, 7-day bar chart, 30-day trend chart, date range filter, and CSV download; imports `nanosecondsToDate` and `dateToNanoseconds` from the restored utility exports in `useQueries.ts`.",
  "frontend/src/pages/InventoryManagement.tsx": "Inventory management page with stats, low-stock alerts, and add/update dialogs; imports InventoryItem type from local types file.",
  "frontend/src/App.tsx": "Root app component with TanStack Router setup for seven routes, wrapped in QueryClientProvider, now includes the Sonner Toaster component mounted at the app root so toast notifications work globally.",
  "frontend/src/components/CustomerForm.tsx": "Reusable customer add/edit form with a useRef double-submission guard; uses the correct `active` boolean field matching the backend Customer type, with an Active/Inactive Switch shown for both add and edit modes.",
  "frontend/src/pages/CustomerManagement.tsx": "Customer management page with search, CSV export, stats cards, and standalone add/edit Dialog components; checks for null return from addCustomer (duplicate name) and shows descriptive error toasts for all failure cases.",
  "frontend/src/pages/DeliveryReports.tsx": "Delivery reports page with log form, stats cards, monthly DeliveryQuantityChart, date filters, CSV export, delivery table, and WhatsApp modal; fixed the `DeliveryQuantityChart` prop name from `deliveries` to `deliveryRecords` to match the component's interface.",
  "frontend/src/lib/localTypes.ts": "Local TypeScript type definitions file; removed the `MilkRecord` and `Cattle` interfaces since they are now available from the backend interface, keeping only `InventoryItem` which remains unsupported in the backend.",
  "frontend/src/pages/MonthlyReports.tsx": "Monthly reports page with month/year selector, delivery summary stats, per-customer breakdown table (now with individual CSV download buttons per row using the csvExport utility and real customer names resolved from the customers list), delivery boy performance table, and milk production stats.",
  "frontend/src/utils/whatsappMessageFormatter.ts": "Utility module with `formatWhatsAppMessage` for generating delivery confirmation text and a new `generateWhatsAppUrl` function that sanitizes a phone number and returns a fully encoded `wa.me` Click-to-Chat URL (or `null` if no phone is provided).",
  "frontend/src/hooks/useClipboard.ts": "Custom React hook providing clipboard copy functionality with isCopied boolean state that auto-resets after a configurable delay, plus error state.",
  "frontend/src/components/WhatsAppMessageModal.tsx": "Modal dialog for WhatsApp delivery notifications; always-mounted and controlled via the `open` prop, shows customer name/phone/quantity chips, a message preview bubble, a green \"Send via WhatsApp\" button that opens a `wa.me` URL, and a copy-to-clipboard fallback; adds a safe date validity check before formatting.",
  "frontend/src/utils/csvExport.ts": "CSV export utility with four functions — existing `downloadCustomerMonthlyCSV` and `exportMilkProductionRecordsToCSV`, plus two new functions: `exportCustomerRecordsToCSV` (exports customer list with ID/name/address/phone/status and summary row) and `exportDeliveryRecordsToCSV` (exports delivery records with customer name resolution and summary row). Refactored to use a shared `triggerDownload` helper.",
  "frontend/src/components/MilkProductionTrendChart.tsx": "New Recharts LineChart component showing aggregated daily milk production for the last 30 days, with responsive container, grid, tooltip, and a \"no data\" empty state.",
  "frontend/src/components/DeliveryQuantityChart.tsx": "New Recharts BarChart component showing daily delivered quantities for the current month, aggregated by day, with responsive container and empty state.",
  "frontend/src/components/CattleHealthChart.tsx": "Recharts PieChart (donut) component showing cattle health breakdown (Healthy/Sick/Recovered) with color-coded segments, legend, and empty state; fixed TypeScript errors by explicitly typing the data array as `HealthDataPoint[]` and using a `Record<string, string>` for the color map."
}