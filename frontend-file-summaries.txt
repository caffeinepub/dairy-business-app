{
  "frontend/src/index.css": "Global stylesheet with warm earthy OKLCH color system; adds a `@media print` block that hides `.print:hidden` elements, resets backgrounds/colors to black-on-white, and ensures table borders and page-break rules are print-friendly.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Nunito/Merriweather fonts, farm color palette tokens, custom card shadows, and all standard shadcn/ui theme mappings.",
  "frontend/src/hooks/useQueries.ts": "Central React Query hooks file with all backend actor calls; replaces the broken `useAddOrUpdateCattle` and `useAddOrUpdateCustomer` hooks (which called non-existent backend methods) with separate `useAddCattle`, `useUpdateCattle`, `useAddCustomer`, and `useUpdateCustomer` hooks that correctly call the actual backend `addCattle`, `updateCattle`, `addCustomer`, and `updateCustomer` methods.",
  "frontend/src/components/Layout.tsx": "App shell with sticky navigation bar (includes all seven nav links with icons), mobile hamburger menu, main content area, and branded footer; restored the logo to the original `ao-farms-logo.dim_320x160.png` asset and removed the redundant text title that was added in Version 16.",
  "frontend/src/components/CattleForm.tsx": "Reusable form for adding/editing cattle records; completely rewritten to match the actual backend `Cattle` type with fields for breed, age (months), daily milk production, health status (healthy/sick/recovered), purchase date, purchase cost, notes, and active status toggle in edit mode.",
  "frontend/src/components/InventoryForm.tsx": "Reusable form component supporting both add-item (name, category, quantity, unit) and update-quantity modes for inventory management.",
  "frontend/src/components/MilkProductionChart.tsx": "Recharts bar chart for 7-day milk production; updated to import `MilkRecord` from the backend interface and aggregate using `quantityLiters` instead of the old local `quantity` field.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard page with AO Farms hero banner, six summary stat cards, 7-day milk chart, recent milk entries table, and today's delivery summary; fixed to use `quantityLiters` (the correct backend `MilkRecord` field) instead of the old local `quantity` field in both the today's milk total calculation and the recent entries table display.",
  "frontend/src/pages/CattleManagement.tsx": "Cattle management page updated to use the new separate `useAddCattle` and `useUpdateCattle` hooks instead of the broken `useAddOrUpdateCattle`; add and edit forms now call the correct backend methods with proper error handling and toast notifications; removed the toggle-active button since the backend doesn't support activeStatus changes via updateCattle.",
  "frontend/src/pages/MilkProduction.tsx": "Milk production page with log form, 7-day chart, and filterable records table; updated to use `quantityLiters` (the real backend field name) instead of the old local `quantity` field in both the mutation call and the total calculation.",
  "frontend/src/pages/InventoryManagement.tsx": "Inventory management page with stats, low-stock alerts, and add/update dialogs; imports InventoryItem type from local types file.",
  "frontend/src/App.tsx": "Root app component with TanStack Router setup for seven routes (/, /cattle, /milk, /inventory, /customers, /deliveries, /reports) all wrapped in the Layout component.",
  "frontend/src/components/CustomerForm.tsx": "Reusable controlled form component for adding/editing customer records with name, address, phone, and active status toggle fields.",
  "frontend/src/pages/CustomerManagement.tsx": "Customer management page updated to use the new separate `useAddCustomer` and `useUpdateCustomer` hooks instead of the broken `useAddOrUpdateCustomer`; add and edit forms now call the correct backend methods with proper error handling and toast notifications; removed the toggle-active button since the backend doesn't support activeStatus changes via updateCustomer.",
  "frontend/src/pages/DeliveryReports.tsx": "Daily delivery reports page with a log-delivery form and filterable records table; fixes the WhatsApp modal by always rendering `WhatsAppMessageModal` (never conditionally) and controlling it via separate `whatsAppModalOpen` boolean and `whatsAppModalData` state, so the Radix Dialog open/close lifecycle works correctly.",
  "frontend/src/lib/localTypes.ts": "Local TypeScript type definitions file; removed the `MilkRecord` and `Cattle` interfaces since they are now available from the backend interface, keeping only `InventoryItem` which remains unsupported in the backend.",
  "frontend/src/pages/MonthlyReports.tsx": "Monthly reports page with month/year selector, delivery summary stats, per-customer breakdown table (now with individual CSV download buttons per row using the csvExport utility and real customer names resolved from the customers list), delivery boy performance table, and milk production stats.",
  "frontend/src/utils/whatsappMessageFormatter.ts": "Utility module with `formatWhatsAppMessage` for generating delivery confirmation text and a new `generateWhatsAppUrl` function that sanitizes a phone number and returns a fully encoded `wa.me` Click-to-Chat URL (or `null` if no phone is provided).",
  "frontend/src/hooks/useClipboard.ts": "Custom React hook providing clipboard copy functionality with isCopied boolean state that auto-resets after a configurable delay, plus error state.",
  "frontend/src/components/WhatsAppMessageModal.tsx": "Modal dialog for WhatsApp delivery notifications; always-mounted and controlled via the `open` prop, shows customer name/phone/quantity chips, a message preview bubble, a green \"Send via WhatsApp\" button that opens a `wa.me` URL, and a copy-to-clipboard fallback; adds a safe date validity check before formatting.",
  "frontend/src/utils/csvExport.ts": "Utility module that generates and triggers a browser download of a per-customer monthly delivery report as a CSV file, including date, quantity, status, delivery boy, notes columns, and a summary section; filename follows the pattern `CustomerName_MonthName_Year.csv`."
}