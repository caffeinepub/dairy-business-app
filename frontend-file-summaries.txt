{
  "frontend/src/index.css": "Global stylesheet with warm earthy OKLCH color system; adds a `@media print` block that hides `.print:hidden` elements, resets backgrounds/colors to black-on-white, and ensures table borders and page-break rules are print-friendly.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Nunito/Merriweather fonts, farm color palette tokens, custom card shadows, and all standard shadcn/ui theme mappings.",
  "frontend/src/hooks/useQueries.ts": "Central React Query hooks file with all backend actor calls; fixed `useAddCustomer` and `useUpdateCustomer` mutations to include the `active: boolean` parameter in both the mutation function signature and the actor method calls, matching the backend interface.",
  "frontend/src/components/Layout.tsx": "App shell with sticky navigation bar (includes all seven nav links with icons), mobile hamburger menu, main content area, and branded footer; updated the logo to use the correct `ao-farms-logo.dim_400x400.png` asset (the user's actual A.O. Farms buffalo-head logo) at height 48px.",
  "frontend/src/components/CattleForm.tsx": "Reusable cattle add/edit form with a `useRef` submission guard; replaced the old `activeStatus: boolean` field with `status: CattleStatus` (using the backend enum), changed the status input from a Switch to a Select dropdown showing \"Active\"/\"Inactive\" options, and made the status field visible for both add and edit modes.",
  "frontend/src/components/InventoryForm.tsx": "Reusable form component supporting both add-item (name, category, quantity, unit) and update-quantity modes for inventory management.",
  "frontend/src/components/MilkProductionChart.tsx": "Recharts bar chart for 7-day milk production; updated to import `MilkRecord` from the backend interface and aggregate using `quantityLiters` instead of the old local `quantity` field.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard page with AO Farms hero banner, six summary stat cards, 7-day milk chart, recent milk entries table, and today's delivery summary; fixed the active cattle count to use `c.status === CattleStatus.active` instead of the non-existent `c.activeStatus` boolean field.",
  "frontend/src/pages/CattleManagement.tsx": "Cattle management page with add/edit dialogs; fixed all references from the old `c.activeStatus` boolean to `c.status === CattleStatus.active` using the correct backend enum, updated the status badge helper, and now passes `status: data.status` in both add and update mutation calls.",
  "frontend/src/pages/MilkProduction.tsx": "Milk production page with log form, 7-day chart, and filterable records table; now includes a \"Download CSV\" button (with Download icon) in the filter controls row that calls `exportMilkProductionRecordsToCSV` with the currently filtered records and cattle data, disabled when no records are present.",
  "frontend/src/pages/InventoryManagement.tsx": "Inventory management page with stats, low-stock alerts, and add/update dialogs; imports InventoryItem type from local types file.",
  "frontend/src/App.tsx": "Root app component with TanStack Router setup for seven routes (/, /cattle, /milk, /inventory, /customers, /deliveries, /reports) all wrapped in the Layout component.",
  "frontend/src/components/CustomerForm.tsx": "Reusable customer add/edit form that correctly uses the `active` boolean field (matching the backend `Customer` type) instead of the old `activeStatus` field; the Active/Inactive switch is now shown for both add and edit modes, defaults to `true` for new customers, and passes `active` in the `onSubmit` callback.",
  "frontend/src/pages/CustomerManagement.tsx": "Customer management page that correctly reads `c.active` (not `c.activeStatus`) for status badge display and active-customer filtering/stats, and passes `active: data.active` in both the add and update mutation calls so the status is properly persisted to the backend.",
  "frontend/src/pages/DeliveryReports.tsx": "Daily delivery reports page with a log-delivery form and filterable records table; fixed two type errors — the filter date now always passes a `Date` object (with a fallback to today) to `useGetDeliveryRecordsByDate`, and the form submission now passes `new Date(formDate)` directly to the mutation (the nanosecond conversion happens inside the hook).",
  "frontend/src/lib/localTypes.ts": "Local TypeScript type definitions file; removed the `MilkRecord` and `Cattle` interfaces since they are now available from the backend interface, keeping only `InventoryItem` which remains unsupported in the backend.",
  "frontend/src/pages/MonthlyReports.tsx": "Monthly reports page with month/year selector, delivery summary stats, per-customer breakdown table (now with individual CSV download buttons per row using the csvExport utility and real customer names resolved from the customers list), delivery boy performance table, and milk production stats.",
  "frontend/src/utils/whatsappMessageFormatter.ts": "Utility module with `formatWhatsAppMessage` for generating delivery confirmation text and a new `generateWhatsAppUrl` function that sanitizes a phone number and returns a fully encoded `wa.me` Click-to-Chat URL (or `null` if no phone is provided).",
  "frontend/src/hooks/useClipboard.ts": "Custom React hook providing clipboard copy functionality with isCopied boolean state that auto-resets after a configurable delay, plus error state.",
  "frontend/src/components/WhatsAppMessageModal.tsx": "Modal dialog for WhatsApp delivery notifications; always-mounted and controlled via the `open` prop, shows customer name/phone/quantity chips, a message preview bubble, a green \"Send via WhatsApp\" button that opens a `wa.me` URL, and a copy-to-clipboard fallback; adds a safe date validity check before formatting.",
  "frontend/src/utils/csvExport.ts": "CSV export utility with two functions — `downloadCustomerMonthlyCSV` for per-customer monthly delivery reports and the new `exportMilkProductionRecordsToCSV` that accepts filtered MilkRecord array and Cattle list, joins cattle breed info, and downloads a dated CSV file named `milk-production-YYYY-MM-DD.csv` with Date, Cattle ID, Breed, Quantity, and Notes columns plus a summary row."
}